# SPEC — 악플 고소 접수 사이트

- 문서 버전: v1.0 (2026-09-24, KST)
- 작성 근거: 대표변호사 인터뷰(입력 15문항 · 출력 9문항 · 운영 4문항)
- 표기: **[확정]** 인터뷰에서 결정된 사항 / **[설계]** 인터뷰 답변을 구현하기 위해 개발 측이 정한 세부사항(변경 요청 가능)

---

## 1. 개요

| 항목 | 내용 |
|---|---|
| 목적 | 악성댓글 피해자가 온라인으로 고소 상담을 접수하고, 변호사가 관리자 화면(전산)에서 접수 건을 확인·관리 |
| 사용자 | ① 고객(접수자) ② 로펌 변호사(관리자, 공용 계정 1개) |
| 배포 | Vercel [확정] |
| 저장소 | DB: Neon Postgres / 파일: Vercel Blob(비공개) [확정] |

---

## 2. 입력 — 고객이 넣는 것

### 2.1 접수자 유형 [확정]

고객은 먼저 아래 3개 중 하나를 선택한다.

| 코드 | 유형 | 설명 |
|---|---|---|
| `individual` | 피해자 본인(개인) | 성인 개인이 본인 명의로 접수 |
| `guardian` | 법정대리인 | 미성년 피해자를 대리하여 접수 |
| `corporation` | 법인·단체 | 회사·기관이 피해자 |

### 2.2 접수자 정보 — 최소정보만 수집, 주민등록번호 수집하지 않음 [확정]

필수 원칙 [확정]: **이름(법인은 상호) · 휴대폰 · 이메일**만 필수, 나머지는 선택.

| 유형 | 필수 | 선택 |
|---|---|---|
| 개인 | 이름, 휴대폰, 이메일 | 생년월일, 주소 |
| 법정대리인 | 대리인 이름, 휴대폰, 이메일, 피해자(미성년자) 이름 | 피해자와의 관계(부/모/후견인/기타) [설계], 피해자 생년월일, 주소 |
| 법인·단체 | 상호, 담당자 이름, 휴대폰, 이메일 | 사업자등록번호, 대표자, 주소 |

- 휴대폰 형식 검증: 숫자·하이픈, 010으로 시작 10~11자리 [설계]
- 이메일 형식 검증 [설계]
- 법인 "담당자 이름"은 연락 대상이므로 필수로 처리 [설계 — "이름 항상 필수" 원칙의 법인 적용]

### 2.3 악플 목록 — 여러 건, "항목 추가" 버튼, 상한 없음 [확정]

악플 1건 = 1항목. 각 항목의 입력란:

| 필드 | 필수 | 형식 |
|---|---|---|
| 내용 원문 | **필수** [확정] | 여러 줄 텍스트, 최대 5,000자 [설계] |
| 플랫폼 | 선택 | **자유 텍스트** [확정] (예: 네이버 뉴스 댓글) |
| URL | 선택 | http/https 주소 형식 검증 |
| 작성일 / 작성시각 | 선택 | 날짜 + (선택)시각. 시각 모르면 날짜만 [설계] |
| 가해자 닉네임 | 선택 | 텍스트 |
| 가해자 계정ID·프로필URL | 선택 | 텍스트 |
| 가해자 실명(알고 있는 경우) | 선택 | 텍스트 |
| 가해자와의 관계 | 선택 | 모름 / 아는 사람 / 모르는 사람 [설계] |
| 고객이 보는 피해 유형 | 선택 | 복수 체크: 모욕 · 허위사실 명예훼손 · 사실적시 명예훼손 · 성적 모욕·성희롱 · 협박 · 개인정보 노출 · 기타 [설계] |
| 현재 게시 상태 | 선택 | 게시 중 / 삭제됨 / 모름 |
| 최초 인지일 | 선택 | 날짜 (친고죄 고소기간 산정 참고용) |
| 증거파일 | 선택 | 2.5 참조 |

- 최소 1건은 있어야 제출 가능. 항목 삭제 버튼 제공(1건 남으면 비활성).

### 2.4 사건 경위 [확정]

- 자유 서술, **선택입력**, 최대 5,000자.

### 2.5 증거파일 — 업로드 선택, 악플 건별 첨부 [확정]

| 규칙 | 값 |
|---|---|
| 허용 형식 | JPG, PNG, WEBP, PDF [확정] — 확장자·MIME·파일 시그니처(매직바이트) 모두 검사 [설계] |
| 파일당 용량 | **4MB** [확정 — Vercel 요청 한도 4.5MB 때문에 10MB에서 축소] |
| 접수당 개수 | **최대 20개** [확정] (모든 악플 항목 합산) |
| 첨부 위치 | 각 악플 항목에 붙임 [확정] |

업로드 방식 [설계]: 파일을 선택하는 즉시 1개씩 서버로 업로드 → 서버는 비공개 저장소에 저장하고 임시 ID 반환 → 최종 제출 시 임시 ID를 악플 항목에 연결. 제출되지 않은 임시 파일은 24시간 후 다음 업로드 시점에 정리.

### 2.6 동의 및 본인확인 [확정]

- 본인인증 **없음**. "개인정보 수집·이용 동의"(필수) 체크만.
- 동의문 초안 [설계 — 변호사 검토 필요]: 수집 항목, 이용 목적(법률상담·사건 검토), 보유기간(로펌이 삭제할 때까지 / 상담 목적 달성 시 파기), 거부권 및 불이익 고지.

### 2.7 제출 후 [확정]

- 화면에 **접수번호만 표시**. 별도 조회·확인 메일 없음.
- 접수번호 형식: `CB-YYYYMMDD-NNNN` (KST 날짜 기준, 일자별 일련번호 4자리) [확정 — 인터뷰 예시안 채택]

---

## 3. 출력 — 변호사가 받는 것

### 3.1 전산 = 이번에 만드는 관리자 화면 [확정]

- 경로: `/admin`
- 계정: **공용 계정 1개** [확정]. 비밀번호는 환경변수 `ADMIN_PASSWORD`.
- 세션: 로그인 시 서명된 쿠키(HttpOnly, Secure, SameSite=Lax), 12시간 만료 [설계]
- 한계(인지 사항): 공용 계정이므로 "누가" 처리했는지 기록되지 않음.

### 3.2 접수 목록 [확정]

- 정렬: 최신 접수 순
- 칼럼: **접수번호 · 접수일시(KST) · 상태 · 접수자명 · 유형 · 연락처**
  - 접수자명: 개인=이름, 법정대리인=대리인 이름(피해자 이름), 법인=상호 [설계]
- 부가 기능: **상태별 필터**만 (전체 / 신규 / 처리중 / 완료, 탭별 건수 표시 [설계])
- 행 클릭 → 상세 화면

### 3.3 상태 [확정]

`신규(new)` → `처리중(in_progress)` → `완료(done)`. 접수 시 자동으로 `신규`. 변호사가 상세 화면에서 자유롭게 변경(역방향 포함).

### 3.4 접수 상세 [확정]

1. 열람: 접수자 정보 전체, 사건 경위, 악플 항목 전체(항목별 첨부파일 목록), 동의 일시
2. **상태 변경**
3. **내부 메모**: 고객에게 보이지 않음. 작성 시각이 찍히는 누적 방식(추가만 가능) [설계]
4. **증거파일 다운로드**: 관리자 인증 후 서버를 통해서만 내려받음(저장소 주소 비노출)
5. **삭제**: 변호사 수동 삭제만 [확정]. 확인 대화상자 후 DB 기록 + 증거파일 완전 삭제. 자동 파기 없음.

### 3.5 신규 접수 알림 — 이메일 [확정]

- 수신 주소·발송 계정 모두 환경변수 [확정]: `NOTIFY_TO`, `NOTIFY_FROM`, `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`
- 미설정 시: 메일 대신 서버 로그에 기록 [확정]
- 메일 내용 [설계]: 제목 `[악플 고소 접수] CB-… 신규 접수`, 본문 = 접수번호·일시·유형·접수자명·악플 건수·관리자 상세 링크. **악플 원문·증거파일은 메일에 넣지 않음**(개인정보 노출 최소화).
- 메일 발송 실패는 접수 자체를 실패시키지 않음 [설계].

---

## 4. 운영·기술

| 항목 | 내용 |
|---|---|
| 프레임워크 | Next.js 16 (App Router, TypeScript) [설계] |
| DB | 운영: Neon Postgres (`DATABASE_URL`) / 로컬·테스트: PGlite(내장 Postgres, 동일 SQL) [설계] |
| 파일 | 운영: Vercel Blob `access: 'private'` (`BLOB_READ_WRITE_TOKEN`) / 로컬·테스트: `./data/uploads` 폴더 [설계] |
| 시간 | DB는 UTC(timestamptz) 저장, 화면은 Asia/Seoul 표시 |
| 테스트 | 단위: Vitest / 종단간(E2E): Playwright — 고객 접수 → 관리자 화면 표시 확인 |

### 4.1 환경변수

| 이름 | 필수 | 용도 |
|---|---|---|
| `ADMIN_PASSWORD` | 운영 필수 | 관리자 공용 비밀번호 |
| `SESSION_SECRET` | 운영 필수 | 로그인 쿠키 서명 키(32자 이상 무작위) |
| `DATABASE_URL` | 운영 필수 | Neon 연결 문자열 (Vercel 연동 시 자동) |
| `BLOB_READ_WRITE_TOKEN` | 운영 필수 | Vercel Blob (연동 시 자동) |
| `APP_BASE_URL` | 선택 | 알림 메일 속 관리자 링크 주소 |
| `NOTIFY_TO`, `NOTIFY_FROM`, `SMTP_*` | 선택 | 알림 메일 |

### 4.2 데이터 모델 [설계]

- `submissions`: id, receipt_no(고유), status, applicant_type, applicant(JSON), narrative, consent_at, created_at, updated_at
- `comments`(악플 항목): id, submission_id, position, content, platform, url, posted_date, posted_time, offender_nickname, offender_account, offender_real_name, offender_relation, harm_types(배열), post_status, first_known_date
- `evidence_files`: id, submission_id(제출 전 NULL), comment_id, original_name, mime, size, storage_key, created_at
- `memos`: id, submission_id, body, created_at
- `receipt_counters`: day(YYYYMMDD), last_no

### 4.3 보안·리스크(인지 사항)

| 리스크 | 수준(추정) | 대응 |
|---|---|---|
| 본인확인 없음 → 허위·장난 접수 | 중 | 변호사가 선별. 필요 시 추후 이메일 인증 추가 |
| 공용 계정 → 처리자 추적 불가·비밀번호 유출 시 전체 노출 | 중 | 강한 비밀번호, 정기 변경 권고 |
| 비로그인 파일 업로드 악용 | 저~중 | 형식·크기·개수 제한, 시그니처 검사, 비공개 저장, 미제출 파일 24시간 정리 |
| 개인정보 영구 보관 → 개인정보보호법 제21조 파기 의무 | 중 | 수동 삭제 기능 제공, 운영 정책으로 주기 점검 권고 |

---

## 5. 범위 밖 (이번에 만들지 않음)

주민등록번호 수집, 본인인증, 고객용 진행상태 조회, 고객 확인 메일, 목록 검색·CSV, 고소장 자동 작성, 고소기간 임박 경고, 개별 계정·권한, 자동 파기, 카카오톡·문자 알림.

---

## 6. 구현 순서 (기능 1개 = 커밋 1개 이상, 완료 시마다 push)

- [ ] F1. 프로젝트 골격(Next.js·TypeScript·테스트 도구)
- [ ] F2. 저장 계층: DB 스키마·접수번호 발급·파일 저장 어댑터(로컬/Vercel)
- [ ] F3. 고객 접수 폼: 유형별 접수자 정보·악플 다건·경위·동의 + 제출 API·검증
- [ ] F4. 증거파일 업로드: 악플 건별, 4MB·20개·형식 검사
- [ ] F5. 접수 완료 화면(접수번호)
- [ ] F6. 관리자 로그인(공용 계정)
- [ ] F7. 관리자 목록(칼럼·상태 필터)
- [ ] F8. 관리자 상세(열람·상태 변경·메모·파일 다운로드·삭제)
- [ ] F9. 신규 접수 이메일 알림
- [ ] F10. Vercel 배포 설정·README(배포 절차)
- [ ] F11. 종단간 테스트: 테스트 접수 → 관리자 화면 표시 직접 확인
